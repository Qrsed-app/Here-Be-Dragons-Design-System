---
title: Button
description: Token-backed button with default, primary, and gold variants.
---

import { Button } from "@/registry/new-york/button/button";

The HBD button reproduces the legacy `.hbd-button` look 1:1 using token-backed
component CSS (folded into the theme), not Tailwind utilities.

## Variants

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Preview>
      <Button variant="default">Default</Button>
      <Button variant="primary">Primary</Button>
      <Button variant="gold">Gold</Button>
    </Preview>
  </Tab>
  <Tab value="Code">
    ```tsx
    import { Button } from '@/components/ui/button';

    <Button variant="default">Default</Button>
    <Button variant="primary">Primary</Button>
    <Button variant="gold">Gold</Button>
    ```

  </Tab>
</Tabs>

## Sizes

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Preview>
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </Preview>
  </Tab>
  <Tab value="Code">
    ```tsx
    <Button size="sm">Small</Button>
    <Button size="md">Medium</Button>
    <Button size="lg">Large</Button>
    ```
  </Tab>
</Tabs>

## Install

```bash
npx shadcn@latest add http://localhost:3001/r/button.json
```
